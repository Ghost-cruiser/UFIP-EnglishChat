<UserControl x:Class="UFIP.EngChat.Components.ChatPanel.Contacts.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UFIP.EngChat.Components.ChatPanel.Contacts"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">

    <Grid>

        <Border Padding="2" BorderThickness="0" Margin="20,20,0,0" Background="{Binding Path=UserColor}" MinHeight="35" MaxHeight="70" MinWidth="35" MaxWidth="70" HorizontalAlignment="Left" Width="70" Height="70" VerticalAlignment="Top">
            <Image x:Name="image" Margin="0,0,0,0" Source="{Binding Path=UserAvatar}"/>
        </Border>

        <Label x:Name="lbl_Username" Content="{Binding Path=Name}" Margin="107,20,0,0" FontWeight="Bold" 
               FontFamily="Mongolian Baiti" FontSize="21.333" HorizontalAlignment="Left" Width="153" Height="45" VerticalAlignment="Top"/>
        <Label x:Name="lbl_Username_Copy" Content="{Binding Path=State}" Margin="107,55,0,0" 
               VerticalAlignment="Top" FontFamily="Monotype Corsiva" FontStyle="Italic" FontSize="16" Height="35" HorizontalAlignment="Left" Width="153"/>

    </Grid>
</UserControl>
